
/**
 * Nofity challenge change to es feeder service
 *
 * @param challengeId challenge id
 */
async function notifyChallengeChange (challengeId) {
  // TODO: notify challenge change to ES
  console.log('Notify challenge change to ES')
}

module.exports = {
  notifyChallengeChange
}
